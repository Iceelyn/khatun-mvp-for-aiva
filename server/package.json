{
  "name": "khatun-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Express backend — the only place that talks to the Anthropic API.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
